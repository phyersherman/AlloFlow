node_modules

.git-token*

# Archive: temp/debug/backup files moved here during cleanup
_archive/

# Security: Never commit build artifacts (they contain baked-in API keys)
prismflow-deploy/build/
prismflow-deploy/.firebase/

# Security: Never commit environment files with secrets
prismflow-deploy/.env
prismflow-deploy/.env.local
prismflow-deploy/.env.production
*.env.local

# Security: Never commit API key files
api key.txt
*api*key*
*.key
*.secret

# Large media files (too large for GitHub)
*.m4a
*.mp4
word_audio_bank_part*.json
audio_bank.json
Sample audiobank.txt
profile-image.jpg
