22940: <label className="block text-lg font-bold text-slate-700 mb-2">{t('wizard.adaptation')}</label>
22941: <p className="text-slate-500 mb-6">{t('wizard.adaptation_desc')}</p>
38496: 1. **Materials Needed:** A comprehensive checklist. You MUST explicitly list every specific resource
38505: - **Explicitly instruct the teacher to use** the "Leveled Text" and "Glossary" provided as part of t
38507: 7. Independent Practice: Instructions for individual work. **Explicitly reference** the "Writing Sca
42049: context.Instruction = "The student has specific interests. Ask if the teacher wants to adapt the Lev
42051: const bridgeMsg = await generateDynamicBridge('Glossary', 'Leveled Text', context);
42073: context.Instruction = "The student has specific interests. Ask if the teacher wants to adapt the Lev
42075: const bridgeMsg = await generateDynamicBridge('Glossary', 'Leveled Text', context);
42080: sendBotMsg("Skipping glossary. Ready for **Leveled Text**?");
42104: const bridgeMsg = await generateDynamicBridge('Leveled Text', 'Visual Organizer', context);
42119: const bridgeMsg = await generateDynamicBridge('Leveled Text', 'Visual Organizer', context);
42139: const bridgeMsg = await generateDynamicBridge('Leveled Text', 'Visual Organizer', context);
45127: MIDDLE SCHOOL ADAPTATION (TRANSITION TO ACADEMIC TEXT):
46701: itemTitle = `Leveled Text (${effectiveGrade})`;
55753: activeView === 'simplified' ? <><BookOpen className="text-green-600" size={20} /> {t('common.tool_si
65853: if (!src) { addToast('No generated content available. Create content first in Text Adaptation.', 'wa
